apiVersion: v2
name: system
description: System chart for Infrastructure CRDs and Controllers (Phase 0)
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: cert-manager
    version: "v1.13.3"
    repository: "https://charts.jetstack.io"
    condition: cert-manager.enabled

